using AutoMapper;
using PrintMatic.Core.Entities;
using PrintMatic.DTOS;

namespace PrintMatic.Helper
{
    //public class ProductDtoPhotoResolved : IValueResolver<Product, ProductDto, string>
    //{
        //private readonly IConfiguration configuration;

        //public ProductDtoPhotoResolved(IConfiguration configuration)
        //{
        //    this.configuration = configuration;
        //}

        //public string Resolve(Product source, ProductDto destination, string destMember, ResolutionContext context)
        //{
        //    if (!string.IsNullOrEmpty(source.))
        //        return $"{configuration["DashboardUrl"]}//Uploads//category//{source.PhotoURL}";

        //    return string.Empty;
        //}
   // }

}
